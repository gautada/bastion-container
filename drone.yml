kind: pipeline
type: exec
name: Container Build - Alpine

platform:
  os: linux
  arch: arm64

steps:
- name: build
  environment:
    ALPINE_VERSION: 3.16.0
    OPENSSH_VERSION: 9.0_p1
  commands:
  - podman build --build-arg ALPINE_VERSION=$ALPINE_VERSION --build-arg OPENSSH_VERSION=$OPENSSH_VERSION --file Containerfile --format docker --label 
revision="$(git rev-parse HEAD)" --label version="$(date +%Y.%m.%d)" --no-cache --tag alpine:build .
- name: publish
  environment:
    OPENSSH_VERSION: 9.0_p1
    DOCKERIO_USERNAME:
      from_secret: username.docker.io
    DOCKERIO_PASSWORD:
      from_secret: password.docker.io
  commands:
   - podman tag bastion:build docker.io/$DOCKERIO_USERNAME/bastion:$OPENSSH_VERSION
   - podman login --username=$DOCKERIO_USERNAME --password=$DOCKERIO_PASSWORD docker.io
   - podman push docker.io/$DOCKERIO_USERNAME/alpine:$OPENSSH_VERSION
  
